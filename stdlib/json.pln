# Module for JSON parsing

parse = { takes text to
    tokenize = {

        i = 0
        c = text[i]

        advance = {
            
        }

        while i < text.length() {
            c = text[i]
            if c == "{" {
                c = c[i + 1]
            }
        }
        tokens = []

    }

    i = 0

    while i < text.length() {
        

        i = i + 1
    }
}